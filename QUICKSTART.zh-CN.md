# 一格安装教程：第一次使用也能跟着做

适用：Windows 10/11 x64、Google Chrome、本机 Codex。当前试用包为 0.5.7 / trial.1。

**你不需要自己输入终端命令。先让 Codex 安装文件，再按本教程点击 Chrome。**
如果 Codex 已告诉你“本机安装完成，还需要加载 Chrome 扩展”，直接从 **第 2 步**开始，不要重复安装。

## 先认清三个位置

| 你看到的窗口 | 用来做什么 | 本教程里往哪里粘贴 |
| --- | --- | --- |
| Codex 聊天窗口 | 告诉 AI 你想做什么 | 安装请求、完成反馈 |
| Chrome 浏览器顶部地址栏 | 打开网页或扩展管理页 | `chrome://extensions` |
| 点击“加载已解压的扩展程序”后弹出的文件夹窗口 | 选择一格所在文件夹 | Codex 给你的、以 `\extension` 结尾的路径 |

**PowerShell / Windows 终端不是选择文件夹的窗口。** 即使它已经打开，也不要把文件夹路径粘贴进去执行。
路径表示“文件放在哪里”，不是“运行什么命令”。

## 第 1 步：让 Codex 完成本机安装

打开电脑上的 Codex，把下面这段话发到聊天框：

> 请帮我安装一格：https://github.com/yigeai2026/yige-chrome 。先阅读 README.md、INSTALL.md、QUICKSTART.zh-CN.md 和 LICENSE.txt，说明试用许可。经我接受后完成本机安装，保留已有模型和 MCP 配置。需要我点击 Chrome 时，一次只指导一个步骤，告诉我在哪里操作、完成后应看到什么。不要让我把文件夹路径当成终端命令执行。

Codex 会先说明试用许可：允许免费使用，禁止修改和转售一格自有代码；第三方组件按各自许可证。
你同意后，回复“我接受试用许可，继续安装”。如果之前已经明确接受，不必重复接受。

等待 Codex 完成下载、文件校验和本机配置。过程中不需要提供 GitHub 密码、API Key 或管理员密码。

**这一阶段完成的标志：** Codex 告诉你本机文件和客户端配置已准备好，并给出“Chrome 要选择的扩展文件夹”。这还不代表浏览器已经连接。

如果 Codex 说当前会话只能操作云端、不能访问你的 Windows 电脑，请先联系维护者确认使用的客户端，不要在云端安装后误以为本机已安装。

## 第 2 步：打开 Chrome 的扩展管理页

1. 打开 **Google Chrome（谷歌浏览器）**。不要在 Edge、Codex 聊天输入框或 PowerShell 中操作。
2. 用鼠标点击 Chrome 窗口**最上方显示网址的长条**，或按 `Ctrl + L`。
3. 输入 `chrome://extensions`，按键盘上的 `Enter`（回车）。

**你应该看到：** 标题为“扩展程序”的管理页面，上面可能列着你已安装的其他扩展。

如果出现搜索结果，说明输入到了网页搜索框。重新点击浏览器最顶部的地址栏再输入。

## 第 3 步：打开开发者模式

1. 在扩展管理页的右上方找到 **开发者模式**。
2. 点击旁边的开关，让它处于开启状态。如果已经开启，不要关掉。
3. 页面顶部会出现 **加载已解压的扩展程序** 按钮。部分版本的中文文案可能略有不同。

**你应该看到：** 可以点击“加载已解压的扩展程序”。

看不到时，先把 Chrome 窗口最大化。如果公司电脑禁止开启开发者模式，请联系管理员或维护者，不要修改系统策略来绕过限制。

## 第 4 步：选择一格的 extension 文件夹（最容易出错的一步）

### 4.1 先取得你自己电脑上的路径

使用 **Codex 本次实际安装后给你的扩展文件夹路径**，通常以 `\extension` 结尾。
每个人的 Windows 用户名、安装位置可能不同，**不要复制别人的 `C:\Users\Administrator\...` 路径**。

如果找不到路径，回 Codex 发送：

> 请检查这台电脑上一格实际安装的位置，给我 Chrome“加载已解压的扩展程序”应选择的完整文件夹路径，确认里面有 manifest.json。请不要重装，也不要让我执行这个路径。

### 4.2 打开文件夹选择窗口

1. 回到 Chrome 的扩展管理页。
2. 点击 **加载已解压的扩展程序**。
3. 此时会弹出一个类似 Windows 文件资源管理器的窗口，用来选择文件夹。

**确认：你现在面对的是这个新弹出的文件夹窗口，不是 PowerShell。**

### 4.3 在文件夹窗口中粘贴路径

1. 先点击这个文件夹窗口，让它成为当前操作窗口。
2. 按 `Ctrl + L`，选中窗口顶部的地址栏。如果快捷键无效，直接点击上方显示文件夹位置的地址栏空白处。
3. 粘贴 Codex 给你的完整扩展路径。只复制路径本身，不要带引号、反引号、序号或“路径：”等说明文字。
4. 按 `Enter`（回车），进入该文件夹。
5. 确认当前文件夹叫 **extension**，再点击右下角的 **选择文件夹**（不同 Windows 版本可能显示“选择”）。

**这段路径只用于选文件夹，不是终端命令。不要在 PowerShell 中回车执行。**

文件夹选择窗口有时只显示子文件夹，看不到 `manifest.json` 是正常的；不需要在里面选择某一个文件。
不要选 ZIP 压缩包，不要选 `node-runtime`，也不要选 `extension` 外面的上一层安装目录。

如果粘贴路径后提示“找不到路径”，先停在这里，让 Codex检查实际目录，不要凭猜测修改用户名或反复重装。

## 第 5 步：确认扩展加载成功

文件夹选择窗口关闭后，回到 Chrome 扩展管理页检查：

- 扩展列表出现名为 **一格** 的卡片。
- 版本显示 **0.5.7**（本教程对应版本）。
- 卡片的启用开关处于开启状态，没有“无法加载扩展程序”的错误弹窗。

**看到这些就说明 Chrome 扩展已加载。** 这时仍需让 Codex 连接并共享一个普通网页。

若已存在另一份一格，不要重复加载多份。把情况告诉 Codex，让它核对已安装版本和目录；不要擅自删除正在使用的扩展。

## 第 6 步：让 Codex 重新加载连接配置

1. 先保存其他正在进行的工作。若 Codex 有任务正在运行，等它结束。
2. 如果你熟悉客户端的 MCP 重连功能，可以重连；否则正常退出并重新打开 Codex。
3. 回到原来的安装会话。如果旧会话仍看不到新工具，再开一个新会话。

可以把这段话发给 Codex，帮助它接上之前的进度：

> 一格的本机安装和 Chrome 扩展加载已经完成。请检查一格的 MCP 连接，不要重复安装。接下来按 https://github.com/yigeai2026/yige-chrome/blob/main/QUICKSTART.zh-CN.md 指导我共享一个测试网页。

**不要退出 Chrome 来代替重启 Codex。** 一格当前版本在 Chrome 关闭后会清空页面共享授权，需要重新共享。

## 第 7 步：共享一个普通测试网页

1. 在 Chrome 新建一个标签页，在地址栏输入 `https://example.com`，回车。这是一个不需要登录的示例页面。
2. 等待网页显示内容。保持当前选中的是这个网页，不要停在 `chrome://extensions`、新标签空白页或设置页面。
3. 点击 Chrome 右上角的 **扩展程序图标**，通常形状像拼图。
4. 在扩展列表中找到 **一格**，点击它。也可点击旁边的图钉，方便以后直接打开。
5. 一格弹窗里会显示当前网页的标题和网址。先核对它们确实属于你要共享的测试页。
6. 点击 **共享此标签页**。

**共享成功的标志：** 按钮变成 **停止共享此标签页**，下方显示已共享的标签数量增加。此时不要再点击一次，否则会取消共享。

弹窗顶部可能显示“一格本地桥接已连接”或“一格本地桥接未连接”。它与“页面是否共享”是两个状态；共享成功后，还要做下一步连接验证。

如果没有找到一格，回到第 5 步检查它是否加载并启用。如果 `example.com` 在你的网络中打不开，可以换一个你能正常访问、无敏感信息的普通网页。

## 第 8 步：让 Codex 验证是否真正可用

回 Codex，发送：

> 我已在 Chrome 中共享测试页。请使用一格先检查连接状态，再列出当前共享标签，只读取这个测试页的标题和网址，不点击、不填写、不导航。请告诉我读取结果；如果未连接，明确卡在哪一层，不要重复安装。

**真正完成的标志：** Codex 通过一格读出你刚才共享的网页标题、网址，且与你的 Chrome 页面一致。
例如使用上面的示例页时，通常会读到标题“Example Domain”、网址 `https://example.com/`。

仅看到“安装完成”“MCP 配置已生成”或“指令已发送”，不能代替这次实际读取验证。

## 出错时，先对照这里

| 看到的现象 | 表示什么 | 下一步怎么做 |
| --- | --- | --- |
| PowerShell 红字“无法将……extension 识别为 cmdlet”，或 `CommandNotFoundException` | 把文件夹路径当成命令执行了；这条报错本身不能证明目录不存在 | 不用重装。从第 2 步进入 Chrome，按第 4 步选择文件夹 |
| Chrome 提示“清单文件缺失或无法读取” | 可能选错目录、未正确解压，或文件不完整 | 让 Codex 检查所选目录里是否有 `manifest.json`；应选 `extension`，不是 ZIP 或上一层目录 |
| Chrome 提示与 `config.js` 有关的加载错误 | 本机配对生成步骤可能未完成，需看具体错误 | 保留 Chrome 原始错误，让 Codex 检查 setup 是否成功，不要复制别人电脑的配置 |
| 弹窗显示“一格本地桥接未连接” | 扩展已能打开，但本地桥接尚未连上 | 让 Codex 先检查 MCP 是否加载、入口路径与版本，不要重复点共享 |
| Codex 说没有一格工具 | 当前会话可能未加载 MCP 配置 | 按第 6 步重连或重启；仍不行时让它检查配置，不直接重装 |
| Codex 列出的共享标签为空 | 当前没有工具可见的共享页面，或 Chrome 重启后授权清空 | 回到普通测试页，按第 7 步重新共享并复查 |
| 提示标签被另一会话占用 | 另一个会话正在使用这个页面 | 先结束或释放原会话的操作，不让两个会话抢同一页 |
| 提示现有配置或 skill 冲突 | 安装器保护了你原有的配置 | 让 Codex 核对一格相关项，保留其他模型/服务，不删除整个配置文件 |

不要把验证码、网站拒绝访问或网络故障一概当成插件安装失败。

## 仍然卡住时怎么反馈

把下面这段填写后发给带你安装的人，或提交到 [问题反馈](https://github.com/yigeai2026/yige-chrome/issues/new?template=bug_report.md)：

> 我在安装教程第 __ 步卡住。  
> 我打开的窗口是：Chrome / 文件夹选择窗口 / Codex / PowerShell。  
> 我刚点击或输入了：__。  
> 实际显示的错误：__。  
> Windows、Chrome、Codex 版本（知道多少填多少）：__。

如果附截图，只截出错区域，遮盖姓名、账号、客户资料和密钥。不要发送完整配置文件、配对文件或整个安装目录。

## 以后每次都要这样安装吗？

不用。同一安装位置未变时，一般只需打开 Chrome 和 Codex，再主动共享任务需要的网页。
关闭 Chrome 后要重新共享；移动安装文件夹或升级版本时，先让维护者/Codex 检查，不直接覆盖正在使用的文件。
不再需要某个页面被操作时，在该页的一格弹窗点击“停止共享此标签页”。
