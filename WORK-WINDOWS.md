# 工作窗口：一次授权，无需逐页共享

适用版本：0.5.8 / trial.1。0.5.7 用户需协调升级客户端、扩展和 skill，不会自动获得此功能。

## 怎么使用

1. 单独打开一个 Chrome 窗口，放入这次任务要操作的网页。
2. 在该窗口点击一格图标，选择 **启用此工作窗口（无需逐页共享）**。这会授权该窗口现有及后续打开的普通 HTTP(S) 网页。
3. 告诉 Agent 目标网址或任务。Agent 发现已授权页面后直接使用，不需要另行绑定或选择 Playwright。
4. Agent 可以在该窗口创建新的任务标签，也可以操作用户在这个窗口中新开的页面。新标签默认在后台打开，页面加载状态另行核验。
5. 要暂时排除一个页面，打开该页的一格弹窗，点 **停止共享此标签页**。该页不会被窗口授权自动加回来；再次点共享可恢复。
6. 完成后点 **停止授权此工作窗口**。该窗口全部页面撤销授权，包括开启窗口模式前曾单独共享的页面。

仍可只使用原来的单页共享。只有单页授权时，Agent 不能借“新建页面”给自己扩大授权范围；需要窗口内新建页面时才启用工作窗口。

## 两个会话怎么用

例如 Codex 使用窗口 A，Grok 使用窗口 B；分别授权两窗口。首次实际操作自动确定窗口归属，新开页面沿用该会话，不逐页争抢。

另一个活跃会话不能操作这个窗口。`yigeai_session release` 对工作窗口按整个窗口释放，等待在途操作结束；手动单页仍按单页释放。客户端断开释放归属，但不会重复执行之前的点击、提交或导航。新连接先发现、复读状态后继续。

若宿主把多个对话复用为同一个 MCP 连接，插件无法从同一连接内区分这些对话；宿主应为独立任务提供独立连接。不要在同一任务页并用两条浏览器控制路径。

## 范围与恢复

- 标签移出授权窗口，窗口授权立即失效；移回后必须使用新快照，旧元素和媒体引用失效。移入另一个授权窗口时沿用目标窗口归属。
- 网页自行打开到另一个窗口，不自动继承原窗口授权。
- 关闭标签或窗口会失效对应授权；Chrome 完全退出再启动需要重新授权。扩展 service worker 休眠不清空授权。
- 新标签页、about:blank 仅允许恢复导航到 HTTP(S)，不能读取内部页面；设置、扩展、本地文件和隐身页面不纳入窗口授权。
- 浏览器授权不等于同意提交、付款等所有业务行为。Agent 仍按用户给定任务执行。
- 页面新建或关闭超时后，先发现当前页面；不要自动重放动作。权限更新也会使队列中旧授权下的命令失效。

## 工具

| 工具 | 用途 |
| --- | --- |
| local_chrome_start | 一次只读返回连接、授权页面、归属、下一步和耗时 |
| local_chrome_list_shared_tabs | 发现单页及工作窗口授权的页面；名称兼容旧客户端 |
| local_chrome_create_tab | 用已授权窗口内 tabId 作为锚点，在相同窗口打开 HTTP(S) 页面 |
| local_chrome_activate_tab | 选中已授权页面；可选聚焦窗口 |
| local_chrome_close_tab | 关闭本会话有权操作的任务页，可能丢失未保存编辑 |

工作窗口开关只存在于扩展弹窗，工具不能为自己授予新窗口权限。工具返回明确授权来源；启动发现不会自动占用或读取所有已授权页面。

## 同批补齐的通用操作

| 能力 | 已实现范围 | 限制 |
| --- | --- | --- |
| 下拉选择 | 原生 select，按实际 value/label 单选、多选，返回已选值 | 自定义菜单用页面控件点击；不猜同名选项 |
| 勾选 | checkbox/radio，返回实际状态，重复设置不再次点击 | radio 取消应选择同组其他选项 |
| 拖拽 | 主文档元素之间拖放；画布内元素相对坐标拖动 | 不把鼠标移动视为业务成功；不跨 iframe |
| 上传 | 明确指定绝对路径，核对文件名/大小，匹配页面 origin | 最多 10 文件、单个 25 MiB、总计 50 MiB；不扫目录；选择不等于服务端上传完成 |
| 下载 | 页面可见 HTTP(S) 链接，保存到 Chrome Downloads，不覆盖同名文件，回读状态/路径/大小 | 不支持 blob 或脚本生成下载；不绕过浏览器警告；不读其他下载记录 |

下载权限是 0.5.8 新增的 Chrome 权限。工具只记住自己发起的下载 ID，不枚举浏览器下载历史；授权撤销后无法继续读取旧下载，但已经交给 Chrome 的下载可能继续。基础流程仍缺跨域 iframe 与脚本下载支持，不能称所有网页自动化已完整覆盖。
