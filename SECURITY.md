# 安全与隐私

扩展使用 tabs、scripting、storage、debugger、alarms、downloads 及网站访问权限；工具操作仍受共享标签、会话和动作检查约束。
主服务仅监听本机回环地址，通过本机生成的配对信息连接。扩展不会请求你的模型 API Key。
页面数据可能发送给你配置的模型服务；客户端可能记录工具实参。不要将隐私页面用于首次测试。

安装包不含维护者配对文件。每机配置、运行数据库、日志与下载包分开存放，不要分发配置后的目录。
SHA256 验证下载与固定发行文件一致，不等同于 Windows 代码签名或全面安全审计。
安装器不修改全局执行策略，也不会绕过企业安全策略。

普通故障通过 Issues 反馈，先脱敏。潜在漏洞请使用仓库 Security → Advisories → Report a vulnerability（本仓库已启用私密漏洞报告）。
若该入口不可用，公开 Issue 只提出需要私下联系，不附利用细节、密钥或客户数据。
首版不保证响应时限，也没有自动安全更新机制。

0.5.8 工作窗口授权覆盖用户选定窗口现有及后续普通网页，其他窗口不自动接入。可排除单页或撤销整个窗口；Chrome 完全退出后需重新授权。
下载工具仅查询同一授权下自己发起的下载，不枚举一般下载历史。撤销权限后，已交给 Chrome 的下载仍可能继续，可在 Chrome 下载界面取消。
文件上传需要明确指定本机文件和目标站点，选择文件可能立即触发网站上传。不要将客户密钥写进 Issue。
