# 更新记录

## 安装教程改进

- 增加面向首次使用者的八步教程：区分窗口、实际文件夹路径、Chrome 加载、重连、共享与读取验收。
- 针对把 extension 路径粘到 PowerShell 的首例反馈，明确路径用途和恢复方式，不误导用户重装。
- 更新 README 和 Agent 指引，要求按用户当前进度一次带一个操作；安装器和试用包内容不变。

## 2026-09-18 · 安装入口首版

- 公开安装和反馈入口，私有开发仓库不变。
- Windows x64 固定版本下载、SHA256 校验、本机配对。
- Codex MCP 与 skill 安装，原配置备份、同名配置冲突保留、重复安装不重置配对。
- Grok 等宿主的 MCP 配置示例。
- 通过真实 trial.1 包的隔离安装测试；不能代替 Chrome 加载、用户共享和宿主重连后的实机验收。

安装器版本与核心版本分别记录；当前试用包核心为 0.5.7。
