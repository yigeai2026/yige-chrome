# 一格安装入口

本仓库是免费试用安装与反馈入口，不是浏览器核心源码。安装任务先读 README.md、INSTALL.md、LICENSE.txt；已有安装时先读 UPGRADE.zh-CN.md。

首次使用的用户按 QUICKSTART.zh-CN.md 逐步带领：每次一个界面动作，写明窗口、操作和成功标志。不得把裸文件夹路径作为可执行命令交给用户。路径需来自本机检查，并明确是在 Chrome 弹出的文件夹窗口粘贴，不是在 PowerShell 执行。

- 用户授权安装且接受许可后，运行本地检查过的 install.ps1；只下载固定 Release 及验证 SHA256 的包。
- 不要求 GitHub 登录、密码、API Key，不访问私有开发仓库。
- 保留用户已有模型、MCP、skill 和浏览器配置；发现冲突先定位，不覆盖或强杀进程。
- 不读取或回传配置文件中无关服务的凭据。不要上传本机配对信息、数据库或私密网页。
- Chrome 首次加载和单页/工作窗口授权由用户明确完成；安装文件成功不等于浏览器连接成功。
- 配置完成后让宿主重连，再先 status、后 list_shared_tabs，仅操作用户共享且匹配任务的标签。
- 默认不更新已安装的一格。升级和卸载要先确保相关任务停止。
- 自有内容采用 LICENSE.txt 的限制许可，第三方组件保留原许可。不要称为 MIT 或开源。
